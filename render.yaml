services:
  - type: worker
    name: vinted-snipe-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python bot.py
    autoDeploy: true